import { describe, expect, it } from "vitest";

// Todo tool was replaced with a new phase/task-based system.
// This test file covers the old TodoItem API which no longer exists.
describe.skip("todo tool (old API)", () => {
	it("is replaced by the new phase/task todo system", () => {
		expect(true).toBe(true);
	});
});
